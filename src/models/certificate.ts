export interface Certificate {
  id: string
  time: string
  timeVisible: boolean
  image: string
  imageVisible: boolean
  index: number
  issuer: string
  issuerVisible: boolean
  name: string
  nameVisible: boolean
  url: string
  visible: boolean
}
