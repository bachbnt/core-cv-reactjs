import { makeStyles } from '@material-ui/core';

export default makeStyles((theme) => ({
  container: {
    width: '100%',
    height: '100%',
  },
  list: {
    paddingBottom: '3rem',
  },
  card: {
    maxWidth: 345,
    height: '100%',
    margin: '3rem 5rem auto',
  },
}));
