import { makeStyles } from '@material-ui/core';

export default makeStyles((theme) => ({
  container: {
    marginBottom: '3rem',
  },
  card: {
    maxWidth: 345,
    height: '100%',
    margin: '3rem 5rem auto',
  },
}));
